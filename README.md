## **Описание задачи**
 
Нужно разработать веб-приложение — личный кабинет продавца с интегрированным AI-ассистентом, который помогает улучшать описания объявлений. Продавец видит список своих объявлений, выбирает нужное и может просмотреть карточку товара, перейти к редактированию и запросить рекомендации от нейросети. Сервис анализирует содержимое карточки товара и предлагает улучшения текста.

## **Бизнес-контекст**
 
Качество описания объявления напрямую влияет на скорость продажи: чем подробнее и привлекательнее текст, тем больше откликов получает продавец. Большинство пользователей Авито — непрофессиональные копирайтеры и зачастую публикуют слишком краткие или неинформативные описания. AI-ассистент помогает продавцу быстро получить конкретные рекомендации и улучшить своё объявление без лишних усилий.\*\*


//Настройка ollama (port: 5051!)

1) устанавливаем легкую олламу(ЛОКАЛЬНО):

	ollama:
	легкая версия -> ollama pull tinydolphin

2) запускаем движок(ЛОКАЛЬНО):

	ollama serve (локально в консоли)

3) переходим в папку 'ollama-backend' и запускаем сервер с олламой:

	node serve

//Подключение сервера

1) npm i в папке /server

2) set PORT=8080 && npm start

// Запуск Фронтенда

1) переходим в папку /client-app

2) npm i - для установки зависимостей

3) npm run dev


-------------------


Примечания
Ollama работает локально
Используется модель: tinydolphin
Основной сервер работает на порту 8080
Ollama backend работает на порту 5051


--------------------
Изменения:
	Был переустановлен package-lock.json для server, так как зависимости не устанавливались.
	wind:
	"
		del package-lock.json
		rd /s /q node_modules
		npm config set registry https://registry.npmjs.org/
		npm install
	"
	
	macOS:
	"
		rm package-lock.json
		rm -rf node_modules
		npm install
	"

	запуск сервера:
	"set PORT=8080 && npm start"

	Добавлена сортировка по цене на сервера